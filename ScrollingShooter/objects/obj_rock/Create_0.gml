// Rock setup – stable: no sideways drift, no spin
hsp = 0;
vsp = 2;          // set to the same vertical speed your enemies use
image_angle = 0;  // no rotation
image_xscale = 1;
image_yscale = 1;

// render behind player/enemies (optional)
depth = 60;       // use a larger depth than enemies if needed
